# โฟลเดอร์ `test` ถูกย้ายแล้ว

โปรเจกต์แดชบอร์ดไม่ได้อยู่ใน `test/` แล้ว

ไปที่รากของ repo แล้วเปิด:

- โฟลเดอร์ `DashboardWebApp\`
- ไฟล์ `DashboardWebApp.sln`
- คำอธิบาย `หาแดชบอร์ดที่นี่.txt`

ถ้าต้องการวางที่ไดรฟ์ C: ให้รัน `คัดลอกไป-C-DashboardWebApp.bat`
